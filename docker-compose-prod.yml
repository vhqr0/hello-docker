version: "2.0"
services:
  flask-dev:
    build: .
    ports:
      - "5000:5000"
    environment:
      - ENV=PROD
    links:
      - dnmonster
      - redis
  dnmonster:
    image: amouat/dnmonster:1.0
  redis:
    image: redis
